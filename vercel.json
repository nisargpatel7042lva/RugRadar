{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo 'Monorepo: dependencies installed per workspace'",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://rugradar-backend-xjf2.onrender.com/api/$1"
    }
  ]
}
